import { numberWithDelimiter } from '../../../helpers/number-with-delimiter';
import { module, test } from 'qunit';

module('Unit | Helper | number with delimiter');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = numberWithDelimiter(42);
  assert.ok(result);
});
