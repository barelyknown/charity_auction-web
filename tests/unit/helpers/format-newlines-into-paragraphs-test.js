import { formatNewlinesIntoParagraphs } from '../../../helpers/format-newlines-into-paragraphs';
import { module, test } from 'qunit';

module('Unit | Helper | format newlines into paragraphs');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatNewlinesIntoParagraphs(42);
  assert.ok(result);
});
