import { formatNewlines } from '../../../helpers/format-newlines';
import { module, test } from 'qunit';

module('Unit | Helper | format newlines');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatNewlines(42);
  assert.ok(result);
});
