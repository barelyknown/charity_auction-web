import { moduleFor, test } from 'ember-qunit';

moduleFor('route:organizations/organization/auctions/auction/auction-items/index', 'Unit | Route | organizations/organization/auctions/auction/auction items/index', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function(assert) {
  var route = this.subject();
  assert.ok(route);
});
