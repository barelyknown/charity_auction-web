import { moduleFor, test } from 'ember-qunit';

moduleFor('route:organizations/organization/auctions/auction/payments/payment/edit', 'Unit | Route | organizations/organization/auctions/auction/payments/payment/edit', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function(assert) {
  var route = this.subject();
  assert.ok(route);
});
