<dl>
  <dt>Physical Address</dt>
  <dd>{{model.physicalAddress}}</dd>
</dl>

<h5>Donations</h5>
{{link-to 'New Donation' 'organization.auction.donations.new'}}
<ul>
  {{#each model.donations as |donation|}}
    <li>{{donation.title}}</li>
  {{/each}}
</ul>

{{outlet}}
