<h4>Auctions</h4>
{{link-to 'New Auction' 'organization.auctions.new'}}
<ul>
  {{#each model.auctions as |auction|}}
    {{#if auction.id}}
      <li>{{link-to auction.name 'organization.auction' auction}}</li>
    {{/if}}
  {{/each}}
</ul>

{{outlet}}
